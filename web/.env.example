# TriServe web environment. Copy to /web/.env for local dev.

# Base URL of the TriServe API. Defaults to /api/v1 (the Vite dev server
# proxies /api -> http://localhost:3000, so the default just works).
VITE_API_BASE_URL=/api/v1
