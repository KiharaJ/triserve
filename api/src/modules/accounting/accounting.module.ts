import { Module } from '@nestjs/common';

/**
 * AccountingModule — skeleton (Task 0.0). Providers, controllers and entities
 * are added by later tasks.
 */
@Module({})
export class AccountingModule {}
